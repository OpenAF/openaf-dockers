name: Scan Images

on:
  workflow_dispatch:
  schedule:
    - cron: '00 7 * * *'

jobs:
  Scan-Images:
    permissions:
      contents: write
      pull-requests: write
      
    runs-on: ubuntu-latest
    name: Scan images
    steps:
    - name: Cache OpenAF runtime
      uses: actions/cache@v3
      with:
        key : oaf-t8
        path: /tmp/oaf
        
    - uses: actions/checkout@v3
      name: Checkout
      
    - uses: openaf/ojob-action@v3
      name: Scan latest
      with:
        ojob: 'ojob.io/sec/genSecBadge'
        args: 'image=openaf/oaf:latest file=.github/sec-latest.svg reportFile=.github/sec-latest.yaml'
        dist: 't8'
        
    - uses: openaf/ojob-action@v3
      name: Scan nightly
      with:
        ojob: 'ojob.io/sec/genSecBadge'
        args: 'image=openaf/oaf:nightly file=.github/sec-nightly.svg reportFile=.github/sec-nightly.yaml'
        dist: 't8'
        
    - uses: openaf/ojob-action@v3
      name: Scan UBI nightly
      with:
        ojob: 'ojob.io/sec/genSecBadge'
        args: 'image=openaf/oaf:ubi-nightly file=.github/sec-ubi-nightly.svg reportFile=.github/sec-ubi-nightly.yaml'
        dist: 't8'
        
    - uses: openaf/ojob-action@v3
      name: Scan UBI latest
      with:
        ojob: 'ojob.io/sec/genSecBadge'
        args: 'image=openaf/oaf:ubi file=.github/sec-ubi-latest.svg reportFile=.github/sec-ubi-latest.yaml'
        dist: 't8'
        
    - uses: openaf/ojob-action@v3
      name: Push
      env :
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        ojob: 'ojob.io/git/hub/contentIn'
        args: 'message="update\ badge" paths=.github/sec-ubi-latest.svg,.github/sec-ubi-nightly.svg,.github/sec-nightly.svg,.github/sec-latest.svg,.github/sec-ubi-latest.yaml,.github/sec-ubi-nightly.yaml,.github/sec-nightly.yaml,.github/sec-latest.yaml'
        dist: t8
        
